{
  "name": "nso-offline-admin-panel",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "start:scheduler": "node scheduler.mjs",
    "check": "node --check server.mjs && node --check scheduler.mjs && node --check public/app.js",
    "test": "vitest run"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "mysql2": "^3.15.0"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
